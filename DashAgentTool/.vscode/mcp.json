{
  "servers": {
    "dashagent": {
      "type": "stdio",
      "command": "npx",
      "args": ["tsx", "packages/mcp-server/src/index.ts"],
      "cwd": "${workspaceFolder}"
    },
    "tableau": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "dotenv-cli", "-e", ".env", "--", "npx", "-y", "@tableau/mcp-server@latest"],
      "cwd": "${workspaceFolder}"
    }
  }
}
